# 2026-09-14

## Frontend: dashboard + playground chat history
- Removed the "Chat with your site" card from `frontend/src/pages/Dashboard.tsx` (the Websites column now spans full width). Also dropped now-unused imports (`Chat`, `ChatProvider`, `CloudUploadOutlined`).
- Recent-conversation list items in Dashboard navigate to `/playground?website=<id>&focus=<session_id>` (full reload via `window.location.href` for reliable deep-linking).
- Hardened `frontend/src/components/Chat.tsx` deep-link handling: the focused conversation's history now loads even if the per-website conversation list is empty (removed the `items.length > 0` gate that previously skipped the direct `getConversationMessages` fallback).

## BUG FIX: Playground empty when opening a recent conversation (ROOT CAUSE = backend)
- `src/site_gpt/app/api/routes.py` referenced `_UUID(...)` in 4 endpoints (chat, list_conversations, get_conversation, delete_conversation) but `_UUID` was never imported → `NameError` → every `/api/conversations*` call returned HTTP 500, so the Playground chat history (and sidebar list) never loaded.
- FIX: added `from uuid import UUID as _UUID` to routes.py imports. Verified with a local uvicorn + minted JWT: `/api/conversations`, `/api/conversations/recent`, `/api/conversations/{session_id}?website_id=` all return 200.
- Debugging recipe for this repo: start `.venv/bin/uvicorn site_gpt.app.main:app --port 8011`; mint a token with `create_access_token` from `site_gpt.app.core.auth` (secret/alg in `core/config`); curl endpoints with `--noproxy '*'` (a proxy intercepts localhost → 502). Read tracebacks in the uvicorn log.
- Verified frontend with `cd frontend && npm run build` (tsc + vite) — passes.

## README: live screenshot gallery (update screenshots)
- Replaced the single `![Example](./site-gpt.png)` with a `## Screenshots` HTML-table gallery (4×2 grid, 8 viewport screenshots) at the top of README.md. Deleted old `site-gpt.png`.
- Captured from the running app via Playwright (installed in an isolated venv `/Users/up5techjacky/.workbuddy-ai/binaries/python/envs/pw`, `playwright install chromium` + `chromium_headless_shell`). Stack was already up: Postgres/Redis via OrbStack, custom OpenAI-compat LLM at :31415, backend `:8000`, frontend `:5173`.
- Seeded demo data for the shots: reset admin `jacky@gmail.com` password to `Demo!1234`; inserted 7 default `settings` rows (assistant_name, widget_* colors/position, greeting, placeholder, suggested_questions) for company UA so the Appearance tab + widget render real values.
- Capture script lives at `/tmp/capture.py` (token injected into localStorage `access_token`, then navigates routes: /login, /, /websites, /websites/<id>, /documents, /users, /settings [Profile/Widget/Appearance tabs], /playground; plus a fake host page `_widget-demo.html` loading `/widget.js`). Screenshots in `screenshots/` (01-login … 12-widget-open).
- Gotcha: Ant Design `ColorPicker` is given a hex string form value but renders as "Transparent" until clicked — pre-existing UI quirk, not touched.
- NOTE: `site-gpt.png` and `screenshots/` are currently uncommitted; `src/site_gpt.egg-info/PKG-INFO` (tracked, autogenerated) no longer references the deleted png.

## FEATURE: multi-source attachments for ExtraDocument (backend foundation)
- User wants extra documents to be importable from many sources (Google Drive, URL, ...), not just local upload.
- Design: make the `Attachment` layer source-agnostic. Every source is normalized into a local file under `uploads/` so `services/parse.py` + `worker.py` (which read `uploads/{file_url}`) stay unchanged. Only *how bytes are fetched* differs.
- Changes:
  - `models.py` `Attachment`: added `source` (default "local") + `source_ref` (nullable) columns.
  - `schemas/extra_document.py` `AttachmentBase`: added `source`/`source_ref` (surfaced in `AttachmentRes` embedded in `ExtraDocumentRes`).
  - NEW `services/sources.py`: provider registry `PROVIDERS` with `UrlProvider` (httpx GET) + `GoogleDriveProvider` (Drive API v3 `?alt=media` with `access_token`; metadata for real filename/mime). `get_provider(source)` factory; add Dropbox/OneDrive/S3 here later.
  - `api/upload.py`: refactored local save into `_save_attachment()` helper; added `POST /api/uploads/url` (body `{url, token?}`) and `POST /api/uploads/google-drive` (body `{file_id, access_token}`). `POST /api/uploads/` (local) kept, now also returns `source`/`source_ref`.
  - Alembic `a1b2c3d4e5f6_attachment_source.py`: adds the 2 columns. Also acts as a MERGE of the two pre-existing heads (`09cf9d60fd13` extra_document + `fb0c17ed1234` chat_feedback) → single head now. (Repo already had 2 heads before this change; I collapsed them.)
  - `.env.example` + `core/config.py`: added optional `GOOGLE_DRIVE_ENABLED` / `GOOGLE_DRIVE_CLIENT_ID` (backend uses client-sent access token, so not required for basic use).
- Verified: `python -c "import site_gpt.app.main; ...sources; ...upload"` → ok; alembic heads single.
- NOT done (next step, user did NOT ask for full connector impl): frontend UI to call `/url` and `/google-drive` from ExtraDocuments.tsx (currently only local `/uploads`).

## FEATURE: frontend UI for multi-source uploads (DONE)
- User said "làm giao diện luôn" -> implemented UI to import extra-document files from URL + Google Drive.
- `frontend/src/pages/ExtraDocuments.tsx`: added "From URL" + "From Google Drive" buttons next to the local upload control. Both open modals; imported files are pushed into the same `fileList` with a `source` field ('url'/'google_drive') and a colored Tag badge in the render list. Reused the existing `fileList -> file_ids -> POST /api/extra_documents` flow (no backend contract change). Split the local Upload to `showUploadList={false}` + a manual render list so all 3 sources show uniformly.
- `frontend/src/utils/googleDrive.ts` (NEW): on-demand loader for Google Identity Services + Picker API (CDN scripts). `pickGoogleDriveFiles(clientId)` returns `[{id, name, token}]`. `extractDriveFileId(link)` parses share links / raw ids. Guarded by `VITE_GOOGLE_DRIVE_CLIENT_ID` (defaults to '' -> Picker button hidden, manual link field still works).
- Backend `services/sources.py` GoogleDriveProvider extended: with `access_token` -> authenticated Drive REST download (existing); WITHOUT token -> public "anyone-with-link" download via `drive.google.com/uc?export=download&id=` handling the virus-scan confirm page. So a pasted public share link works with zero OAuth setup.
- README: added env-var rows + "Multi-source extra documents" section. frontend/.env got `VITE_GOOGLE_DRIVE_CLIENT_ID=` (commented usage).
- FIX: pre-existing latent TS error in `frontend/src/components/Chat.tsx:337` (`onClick={(e)=>...}` missing type under noImplicitAny) was blocking `npm run build`. Added `(e: any)` to unblock the build. Unrelated to the feature.
- Verified: `cd frontend && npm run build` passes (tsc + vite). Backend imports ok; alembic single head `a1b2c3d4e5f6`.
