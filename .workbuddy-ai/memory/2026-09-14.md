# 2026-09-14

## Frontend: dashboard + playground chat history
- Removed the "Chat with your site" card from `frontend/src/pages/Dashboard.tsx` (the Websites column now spans full width). Also dropped now-unused imports (`Chat`, `ChatProvider`, `CloudUploadOutlined`).
- Recent-conversation list items in Dashboard navigate to `/playground?website=<id>&focus=<session_id>` (full reload via `window.location.href` for reliable deep-linking).
- Hardened `frontend/src/components/Chat.tsx` deep-link handling: the focused conversation's history now loads even if the per-website conversation list is empty (removed the `items.length > 0` gate that previously skipped the direct `getConversationMessages` fallback).

## BUG FIX: Playground empty when opening a recent conversation (ROOT CAUSE = backend)
- `src/site_gpt/app/api/routes.py` referenced `_UUID(...)` in 4 endpoints (chat, list_conversations, get_conversation, delete_conversation) but `_UUID` was never imported → `NameError` → every `/api/conversations*` call returned HTTP 500, so the Playground chat history (and sidebar list) never loaded.
- FIX: added `from uuid import UUID as _UUID` to routes.py imports. Verified with a local uvicorn + minted JWT: `/api/conversations`, `/api/conversations/recent`, `/api/conversations/{session_id}?website_id=` all return 200.
- Debugging recipe for this repo: start `.venv/bin/uvicorn site_gpt.app.main:app --port 8011`; mint a token with `create_access_token` from `site_gpt.app.core.auth` (secret/alg in `core/config`); curl endpoints with `--noproxy '*'` (a proxy intercepts localhost → 502). Read tracebacks in the uvicorn log.
- Verified frontend with `cd frontend && npm run build` (tsc + vite) — passes.

## README: live screenshot gallery (update screenshots)
- Replaced the single `![Example](./site-gpt.png)` with a `## Screenshots` HTML-table gallery (4×2 grid, 8 viewport screenshots) at the top of README.md. Deleted old `site-gpt.png`.
- Captured from the running app via Playwright (installed in an isolated venv `/Users/up5techjacky/.workbuddy-ai/binaries/python/envs/pw`, `playwright install chromium` + `chromium_headless_shell`). Stack was already up: Postgres/Redis via OrbStack, custom OpenAI-compat LLM at :31415, backend `:8000`, frontend `:5173`.
- Seeded demo data for the shots: reset admin `jacky@gmail.com` password to `Demo!1234`; inserted 7 default `settings` rows (assistant_name, widget_* colors/position, greeting, placeholder, suggested_questions) for company UA so the Appearance tab + widget render real values.
- Capture script lives at `/tmp/capture.py` (token injected into localStorage `access_token`, then navigates routes: /login, /, /websites, /websites/<id>, /documents, /users, /settings [Profile/Widget/Appearance tabs], /playground; plus a fake host page `_widget-demo.html` loading `/widget.js`). Screenshots in `screenshots/` (01-login … 12-widget-open).
- Gotcha: Ant Design `ColorPicker` is given a hex string form value but renders as "Transparent" until clicked — pre-existing UI quirk, not touched.
- NOTE: `site-gpt.png` and `screenshots/` are currently uncommitted; `src/site_gpt.egg-info/PKG-INFO` (tracked, autogenerated) no longer references the deleted png.
