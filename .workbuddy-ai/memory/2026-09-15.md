# 2026-09-15

## Tách cấu hình Chat model / Embedding model thành độc lập
- Thêm `EMBEDDING_AI` (mặc định = `LLM_AI`, tương thích ngược) và `EMBEDDING_DIMENSIONS` (mặc định 1536) vào `src/site_gpt/app/core/config.py`.
- `services/llm.py::get_embedding_model()` giờ switch trên `EMBEDDING_AI` thay vì `LLM_AI`; `_embedding_dimensions` trả về `EMBEDDING_DIMENSIONS` cho text-embedding-3*/auto.
- `models.py`: `Vector(EMBEDDING_DIMENSIONS)` thay vì hardcoded 1536.
- Docs: `.env.example`, `README.md` (bảng biến + mục "Embedding dimension caveat" cập nhật), `AGENTS.md`.
- Verify: import OK; `alembic check` không báo diff cột embeddings (migration-free ở default 1536).
  Hai diff còn lại (`chat_feedback` FK, `users.reset_token_expires_at` type) là pre-existing, không liên quan — không đụng.
- Lưu ý: đổi `EMBEDDING_DIMENSIONS` (vd bge-m3=1024) cần migration + re-ingest.
