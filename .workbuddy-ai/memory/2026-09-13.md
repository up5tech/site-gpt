# 2026-09-13

- Redesigned `frontend/src/pages/Dashboard.tsx`: added 4 stat cards (Total/Indexed/Ingesting/Team), greeting header + "Add Website" quick action, moved Chat into right column beside Websites, added icons + "View all" links to card headers. Stats computed client-side from `/websites` + `getCompanyUsers`. Kept B&W theme, added emerald/amber/blue accent chips. Verified with `npm run build` (passed).
- Added backend `GET /api/websites/stats` (website.py) + `DashboardStats` schema (schemas/app.py): returns total_websites, indexed_websites (ingest_status=completed), ingesting_websites (processing), team_members, scoped per company. Route placed before `/{website_id}` to avoid path-param capture.
- Frontend: added `DashboardStats` type + `getDashboardStats()` helper (utils/api.ts); Dashboard now pulls stat cards from `/stats` instead of fetching 100 items; `UserTable` gained `compact` prop (pagination 5, hides phone col, size small) used on dashboard. Verified: `npm run build` OK + backend import OK.
- Extended `/stats`: added `total_documents` (Document joined to company websites) and `total_chat_messages` (ChatMessage joined to company websites); `team_members` now counts only `status='active'`. Dashboard shows 5 responsive stat cards (Total Sites, Indexed Pages, Ingesting, Conversations, Team Members) via flex grid + MessageOutlined. Verified build + import OK.
- Stat cards on Dashboard are now clickable (Link): Total Sites/Indexed Pages/Ingesting -> /websites, Conversations -> /playground, Team Members -> /users; added clean skeleton block for the number while loading (replaced Statistic shimmer). Verified build OK.
- Added clickable affordance to dashboard stat cards: ArrowRightOutlined chevron (hover -> opacity 1 + translateX) via `.stat-card-link` rule in App.less, plus aria-label on the wrapping Link. Verified build OK.
- Confirmed /websites already has Add Website button + modal, so dashboard "Add Website" CTA is functional (2-step navigation).
- WebsiteTable rows are now clickable: onClick navigates to /websites/:id (detail), cursor pointer. Extends the dashboard navigation pattern (stat cards already link). Verified build OK.
- Decided NOT to change Chat component height for Websites/Chat column alignment because Chat is shared with Playground and the fix can't be visually verified without a browser.
