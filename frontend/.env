VITE_BACKEND_URL=http://localhost:8000
# Optional: enable the native "Pick from Google Drive" picker button.
# Create an OAuth client id in Google Cloud (Drive API + Picker API enabled,
# origin http://localhost:5173 authorized) and paste it here. Without it, you
# can still import publicly-shared Drive links via the manual link field.
VITE_GOOGLE_DRIVE_CLIENT_ID=
